import React from 'react';
import { Typography } from 'antd';

const { Title } = Typography;

const PlatformEntityMappingPlaceholder: React.FC = () => {
  return (
    <div style={{ padding: '24px' }}>
      <Title level={2}>Platform Entity Mapping</Title>
      <p>This page is under development.</p>
    </div>
  );
};

export default PlatformEntityMappingPlaceholder;
